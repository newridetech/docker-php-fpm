version: 2
jobs:
  build:
    docker:
      - image: docker:latest
    working_directory: ~/repo
    steps:
      - checkout
      - setup_remote_docker
      - restore_cache:
          keys:
            - v1-{{ .Branch }}
          paths:
            - /tmp/php.tar
      - run: |
          set +o pipefail
          docker load -i /tmp/php.tar | true
      - run: docker build --cache-from=php -t php .
      - run: docker save -o /tmp/php.tar php
      - save_cache:
          key: v1-{{ .Branch }}-{{ epoch }}
          paths:
            - /tmp/app.tar
      - run: docker push newride/php
