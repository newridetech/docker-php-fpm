version: 2
jobs:
  build:
    docker:
      - image: docker:latest
    working_directory: ~/repo/7.1
    steps:
      - checkout
      - setup_remote_docker
      - run: docker build
      - run: docker save php
